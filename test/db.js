// Generated by CoffeeScript 1.10.0
var assert, db, describe, globalize, it, ref;

ref = require('mocha'), describe = ref.describe, it = ref.it;

assert = require('chai').assert;

globalize = require('../globalize');

db = require('../db');

describe('DI of models', function() {
  var models;
  models = ['user', 'exam'];
  return models.forEach(function(model) {
    return it('type of model ' + model + ' should be object', function() {
      var expected;
      expected = typeof db[model];
      return assert.equal(expected, 'object');
    });
  });
});
