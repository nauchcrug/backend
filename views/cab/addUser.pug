extends layout
block content
  section.content-header
    h1 Форма регистрации
    hr.under-heading
  section.content
    form(role="form", name="reg", enctype="multipart/form-data")#add-task
    fieldset.form-group: .input-group
        span.input-group-addon Ваше логин:
        input(autofocus, required, name="name", type="text", placeholder="Anatoliy").form-control
    fieldset.form-group: .input-group
        span.input-group-addon Ваше имя:
        input(autofocus, required, name="firstname", type="text", placeholder="Анатолий").form-control
    fieldset.form-group: .input-group
        span.input-group-addon Ваша фамилия:
        input(required, name="secondname", type="text", placeholder="Умнов").form-control
    fieldset.form-group: .input-group
        span.input-group-addon Ваша электронная почта
        input(required, name="email", type="text", placeholder="um@yandex.ru").form-control
    fieldset.form-group: .input-group
        span.input-group-addon Пароль
        input(required, name="password", type="text", ).form-control
    fieldset.form-group: .input-group
         input(required, type='checkbox', name='check', value='1').form-control
    | Согласны с правилами проекта
      button(type="submit", name="send").btn.btn-default.btn-flat#send-task Отправить
  .modal.fade#message(role="dialog", tabindex="-1", aria-labeledby="#message-label"): .modal-dialog(role="document"): .modal-content
    //.modal-header
      //button(type="button", data-dissmiss="modal").close &times;
      h4.text-center Результат регистрации:
    .modal-body: h1.text-center#message-body

block script
  //script(defer, src="//cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-MML-AM_CHTM")
  script(async, src="/cab/static/tinymce.min.js")
  //script(async, src="/cab-add.js")
  style.
    hr.under-heading{
      background:#999;
      margin-top:5px;
      margin-bottom:0;
      padding: 0;
      height: 1px;}
    .hidden{visibility:hidden;display:none;opacity:0;}
