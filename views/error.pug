doctype html
html
  head
    title Ошибка
    meta(name="viewport" content="width=device-width")
    link(rel="stylesheet" href="http://fonts.googleapis.com/css?family=Roboto:300,400,500,700" type="text/css")
  body
    .page-wrap
      .message-wrap
        //.message= err.message
        img(src='/images/email.png')
        .message= err.status === 404 ? 'Страница не найдена' : 'Произошла ошибка'
        .description.
          Вы можете попробовать вернуться на главную страницу, там много полезной информации =)
    if !production
      .stack-wrap
        hr.dotted(style="opacity:0;")
        pre.stack= err.stack
    style: include assets/error.css
